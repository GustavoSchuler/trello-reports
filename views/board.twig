{% extends 'layout.twig' %}

{% block body %}
    <ul>
        {% for b in boards %}
            <li>
                <a href="?board={{ b.id }}">{{ b.name }}</a> (<a href="{{ path('GET_board_id', {id: b.id}) }}">Lists</a>)
            </li>
        {% endfor %}
    </ul>
{% endblock %}
